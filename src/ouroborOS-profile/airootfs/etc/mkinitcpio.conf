# mkinitcpio.conf — ouroborOS live ISO
# btrfs must be in both MODULES and HOOKS for immutable root support

MODULES=(btrfs)
BINARIES=()
FILES=()
HOOKS=(base udev autodetect microcode modconf kms keyboard keymap consolefont block filesystems fsck)
